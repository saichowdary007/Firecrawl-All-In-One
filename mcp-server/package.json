{
  "name": "firecrawl-mcp-server",
  "version": "1.0.0",
  "description": "MCP Server for Firecrawl web scraping and data extraction",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "clean": "rm -rf dist",
    "setup": "./setup-cursor.sh",
    "test": "npm run build && timeout 5s npm run start || echo 'Server started and stopped for testing'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "axios": "^1.6.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
